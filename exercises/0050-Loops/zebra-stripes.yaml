title: "Zebra Stripes"
descriptionMd: |
  # Zebra Stripes

  In this exercise, you must draw a zebra stripe pattern. Your program's output must look **exactly** like the image below:

  ![Zebra Stripes](https://github.com/rstropek/ts-web-playground/blob/main/exercises/0050-Loops/zebra-stripes.png?raw=true)

  **Note:** You are only allowed to use a **single** _while_ loop.

  **Tip:** Remember the _modulo_ operator (`%`)? It will be very useful here.

sampleSolution: "https://raw.githubusercontent.com/rstropek/ts-web-playground/refs/heads/main/exercises/0050-Loops/zebra-stripes.ts"
files:
  "index.ts":
    content: |
      function setup() {
          const SIZE = 400;  // Size of canvas
          const STRIPE_THICKNESS = 50; // Thickness of the stripes

          createCanvas(SIZE, SIZE);
          background("black");

          noStroke();

          // <<< Add your code here
      }

    isEditable: true
  "index.html":
    content: |
      <!DOCTYPE html>
      <html lang="en">
        <head>
          <meta charset="UTF-8" />
          <meta name="viewport" content="width=device-width, initial-scale=1.0" />
          <script src="{{p5Source}}"></script>
          {{topScripts}}
          </head>
        <body>
          {{bodyScripts}}
        </body>
      </html>
    isEditable: false
